# Phase 3: RAG (Ollama + ChromaDB)

# Paths (relative to project root)
reports_dir: models/team_reports
chroma_persist_dir: data/chroma
feature_importance_path: models/feature_importance.json
model_metadata_path: models/model_metadata.json

# Ollama models (must be pulled: ollama pull nomic-embed-text && ollama pull llama3.2)
embedding_model: nomic-embed-text
llm_model: llama3.2

# Retriever
collection_name: lacrosse_team_reports
retriever_k: 5
